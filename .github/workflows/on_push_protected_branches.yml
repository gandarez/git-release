name: "Create Release"

on:
  push:
    branches:
      - "develop"
      - "master"

env:
  GO_VERSION: "1.15.x"

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout"
        uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: ${{ env.GO_VERSION }}
      - name: "Pull dependencies"
        run: go mod vendor
      - name: "Unit tests"
        run: make test
      - name: "Linter"
        run: make lint
  
  test-windows:
    name: Test Windows
    runs-on: windows-latest
    steps:
      - name: "Checkout"
        uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: ${{ env.GO_VERSION }}
      - name: "Pull dependencies"
        run: go mod vendor
      - name: "Unit tests"
        run: make test
      - name: "Linter"
        run: make lint
  
  release:
    name: Release
    runs-on: ubuntu-latest
    needs: [test, test-windows]
    steps:
      - name: "Checkout"
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: "Calculate semver tag"
        id: semver-tag
        uses: wakatime/semver-action@v1.0.0
        with:
          prerelease_id: "alpha"
          debug: "true"
      - name: "Create tag"
        uses: actions/github-script@v3
        with:
          github-token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
          script: |
            github.git.createRef({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: "refs/tags/${{ steps.semver-tag.outputs.semver_tag }}",
              sha: context.sha
            })
      - name: Changelog
        uses: mikepenz/release-changelog-builder-action@v1.7.1
        id: changelog
        # with:
        #   fromTag: ${{ steps.semver-tag.outputs.previous_tag }}
        #   toTag: ${{ steps.semver-tag.outputs.semver_tag }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: "Create release"
        uses: actions/github-script@v3
        with:
          github-token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
          script: |
            github.repos.createRelease({
              owner: context.repo.owner,
              repo: context.repo.repo,
              tag_name: ${{ steps.semver-tag.outputs.semver_tag }},
              name: ${{ steps.semver-tag.outputs.semver_tag }},
              body: ${{ steps.changelog.outputs.changelog }},
              prerelease: ${{ steps.semver-tag.outputs.is_prerelease }}
            })
          debug: true
